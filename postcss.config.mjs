// Tailwind CSS v4 is handled by the @tailwindcss/vite plugin directly.
// This empty config prevents Vite from crawling up to a parent directory
// and picking up a stale postcss.config.mjs that references tailwindcss v3.
export default {};
